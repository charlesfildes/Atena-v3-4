steps:
- name: 'mobiledevops/android-sdk-image:34.0.0'
  entrypoint: 'bash'
  args:
    - '-c'
    - |
      mkdir -p /tmp/src /tmp/.gradle
      cp -r . /tmp/src
      cd /tmp/src
      chmod +x gradlew
      ./gradlew assembleDebug --no-daemon -g /tmp/.gradle
      mkdir -p /workspace/app/build/outputs/apk/debug
      cp -r app/build/outputs/apk/debug/* /workspace/app/build/outputs/apk/debug/

artifacts:
  objects:
    location: 'gs://arched-science-453116-h5_cloudbuild/apks/'
    paths:
      - 'app/build/outputs/apk/debug/*.apk'
